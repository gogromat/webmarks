$('#flash').html("<%= escape_javascript(raw(flash_display)).html_safe %>");
$('#userLinkage').html("<%= escape_javascript(render partial: 'users/user_linkage',
                                                     locals: { user_linkage: @user_linkage } ).html_safe %>");