<% text = "delete" if local_assigns[:text].nil?  %>
<%= link_to text, linkage_path(linkage_item),
                      method: :delete,
                      remote: true,
                      class: 'deleteLinkage fr' %>